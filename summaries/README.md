# Sweep summaries

`sweep_summary_v4.md` is the headline file. Every figure in the companion article traces to it.

`sweep_summary.md` (v1), `sweep_summary_v2.md`, and `sweep_summary_v3.md` are superseded. They are kept because the four bugs documented in the top-level README were found by comparing those passes to v4, not by discarding them.
